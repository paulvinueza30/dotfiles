return {
  "m4xshen/hardtime.nvim",
  dependencies = { "MunifTanjim/nui.nvim" },
  opts = {
    -- All options are enabled by default
    -- You can disable features here if you want
    -- For example, to disable the hint messages:
    -- hint = false,

    -- To allow arrow keys, you can disable them from being restricted:
    -- disabled_keys = {
    --   ["<Up>"] = false,
    --   ["<Down>"] = false,
    --   ["<Left>"] = false,
    --   ["<Right>"] = false,
    -- },
  },
}
